运算符~ | 名称 | 作用 |示例 |等价于
:-:|:-:|:-:|:-:|:-:
+|加法运算符|加|6+9
-|减法运算符|减|9-2
*|乘法运算符|乘|4*3
/|除法运算符|除|9/3
%|取余运算符|取余数|8%3=2
**|幂|几次方|2**10=1024
num++|递增|先取后加|
++num|递增|先加后取|
num--|递减|先取后减|
--num|递减|先减后取|
+=|加法赋值|右边的数值加上左边的变量，然后返回新的值|x=3;x+=4;|x=3;x=x+4;
-=|减法赋值|左边的数值减上右边的变量，然后返回新的值|x=6;x-=4;|x=6;x=x-4;
*=|加法赋值|右边的数值*加*上左边的变量，然后返回新的值|x=3;x*=4;|x=3;x=x*4;
/=|加法赋值|右边的数值除上左边的变量，然后返回新的值|x=3;x/=4;|x=3;x=x/4;
===|严格等于|测试左右值是否相同|5===2+4
!==|严格不等于|测试左右值是否不相同|5!==2+3
<|小于|左是否小于右|10<6
>|大于|左是否大于右|10>20
<=|小于或等于|左是否小于或者等于右|3<=2
>=|大于或等于|左是否大于或者等于右|5>=4
字符串|字符串|用''或者""包裹|'123'   "123"|用+可以连接字符串
alert()|弹框|屏幕上显示输出|纯粹输出
prompt()|弹框|屏幕上可以输入内容|有返回的东西例如空格和null
confirm()|弹框|屏幕显示|返回true或false
`${}`|字符串连接|不适用+实现变量和字符串的连接|`${name}的年龄是${age}岁`   let link=\``<a href="${url}">${name}</a>`\`;   document.write(link);|\`（去掉）`'<a href="'+url+'">'+name+'</a>'`;
?:|三元（目）运算符或条件运算符|判断条件?条件为true时的值:条件为false时的值|这个可以跟随html中的位置改变位置
new String()|字符串对象|把字符串变成对象|let s=new String('abc');typeof s; 值：'object'|let s='abc'; s; 值：'abc'
length|字符串长度|可以获取字符串的长度，如果设为0，没有效果|s.length   s[2]='c' s[s.length-1]=最后一个字符
Math.random|随机数|随机出现一个数|Math.randow()*26 [0,26)
Math.floor|小于等于最大整数|获取小于等于的最大整数|Math.floor(Math.randow()*26);
indexOf()|查找子字符串第一次出现的位置|-1表示没找到，所在索引出来之后表示找到了，索引为位置|let x=s.indexOf('a');
lastIndexOf()|查找子字符串最后一次出现的位置|-1没找到，索引出现表示位置|let y=s.lastIndexOf('a');
slice(0,3)|提取字符|[开始，结束)|let file='abc.txt';let n=file.lastIndexOf('.');let ext=file.slice(n);(扩展名)|补充：如果第二个参数是负数，是从后面开始。
toLowerCase()|字母小写
toUpperCase()|字母大写
replace()|替换字符|第二个参数覆盖第一个参数|let x=s.replace('123','000');(替换第一次出现的123)let x=s.replace(/123/g,'000');（替换全部，正则表达式）
数组|let names=new Array('张三','lisi','tom');let names=['zhangsan','lisi','wangwu'];
length|数组个数|如果设为0，相当于删除
split()|将字符串变成数组|','是把逗号分割成多个元素'm'是加一个空的元素|let s='zhangsan,lisi,tom';let a=s.split(',');let a=s.split(',');let x=a.join('');let a=s.split();
join()|将数组变成字符串|
push()|添加元素|可以添加一个值以及更多值|s.push(1);返回值为新的数组长度
pop()|删除元素|可以删除元素没有值，每次只会删除一个元素，从末尾开始|s.pop();返回删除元素。
unshift()|和push一样左右，只是添加在开头
shift()|和pop一样，只是从开头开始删